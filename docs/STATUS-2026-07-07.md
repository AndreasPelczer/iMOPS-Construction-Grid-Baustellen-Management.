# iMOPS — Statusübergabe · Stand 7.7.2026

## Kernergebnis heute

Der Mops liest jetzt die Town-&-Country-PDFs — end-to-end. T&C-Leistungsverzeichnisse
kommen als „ComponentOne C1Pdf" mit kaputtem Text-Layer (CID-Fonts ohne ToUnicode →
Textextraktion = Buchstabensalat). Gelöst über einen Vision-Fallback im Backend; das
Ergebnis fließt bis in die iOS-App (241 LV-Positionen der Referenzdatei „aura125").

---

## 1) Backend — Vision-Fallback (Repo `mops-api`, läuft auf der Box)

Branch `feature/vision-fallback-c1pdf` — auf origin gepusht.

- `complete_json_vision(images, prompt)` in Claude- und OpenAI-Client (vorher text-only)
- neues `api/services/pdf_vision.py`: C1Pdf-Erkennung (Producer-Shortcut) + Müll-Text-Heuristik,
  Seiten-Rendering via PyMuPDF/fitz (kein Poppler!), parallele Block-Extraktion → `ExtractPlanResult`
- `/extract-plan`: neuer `allow_vision`-Schalter (ohne → 422-Hinweis, mit → Vision)
- Rate-Limit-Härtung (Semaphore max 2 + 429-Backoff) — nötig wegen gpt-4.1 30k TPM
- `menge` als Float (deutsch „35,153" → 35.153) für iOS-Kompatibilität
- Politur: Duplikate + Titelzeilen bereinigen
- Commits: `9657127` · `e8d339e` · `6abcfc3` · `1259e1b`
- Validiert LIVE: aura125 (80 S.) → gpt-4.1 → 241/241 posNr, lückenlos, ~125 s
- Provider aktiv: `EXTRACT_PROVIDER=openai` (gpt-4.1)
- ⚠️ Live-Dienst läuft auf `6abcfc3` — die Politur `1259e1b` ist committet/gepusht, aber noch
  nicht live → braucht `sudo systemctl restart mops-api`

## 2) iOS — LV-Import aus JSON-Datei (Repo iOS, Branch `codex/lv-baustein-search`)

Gepusht.

- `JSONDocumentPicker` + Button „LV aus JSON-Datei" + `importJSONFiles`
  (Decode → `ExtractPlanMapper.toParsed` → Review-Liste)
- `ExtractPlanResult.init(from:)` defaultet fehlende Felder (robust für externe JSONs)
- Drift-Regel: `app_bedienung.yaml`-Eintrag
- Regressionstest `LVJSONImportTests.swift`
- Verifiziert: Andreas hat interaktiv 241 Objekte geladen ✅ · Build grün · Test SUCCEEDED
- Commits: `9ed635e` · `0e9a8d4`

## 3) iOS — UI-Umbau + Bugfixes (Branch `codex/lv-baustein-search`)

Committet, **NICHT** gepusht, noch nicht auf dem Gerät geprüft.

- Baustellen-Detail: 13 Karten → 5 einklappbare Gruppen (Übersicht & Wetter · Pläne & Unterlagen ·
  LV & Kalkulation · Gewerke & Ausführung · Mängel). Risikoarm: Karten unverändert, nur in
  `DisclosureGroups` gehüllt.
- Plan-Lösch-Bug gefixt: Swipe griff im `VStack` nicht → sichtbarer Papierkorb +
  Bestätigungsdialog + Datei wird jetzt auch von der Platte gelöscht.
- Tab-Icon-Fix: „iMOPS Planer"-Tab hatte kein Symbol (`pencil.and.rulers` existiert nicht →
  korrigiert auf `pencil.and.ruler`).
- Commits: `0f283c4` · `48f15ca` · Builds grün (iPhone 17 Pro Max / iOS 26.2)

---

## Offene Punkte / nächste Schritte

1. Backend-Restart → Politur `1259e1b` live.
2. iOS auf dem Gerät ansehen (5 Gruppen + Tab-Icon); Andreas hat bezahltes Apple-Dev-Konto
   (Team `F75D7PGFTD`) → Install bleibt (kein 7-Tage-Spuk).
3. UI-Commits pushen (`0f283c4`, `48f15ca`) — nur auf Andreas' OK.
4. PRs bewusst nicht erstellt (nur push).
5. Ggf. weitere UI-Änderungen (Andreas hatte „einige" angekündigt).

## Regeln (für die Fortsetzung)

- Deutsch, kollegial. Vor jedem Patch `.backup_*`. Nie ohne Andreas' OK pushen/PR.
  Destruktives nur mit Bestätigung + Folgen benennen. Keine Kundendaten ins Repo.
  Commit-Messages tragen Buch-Kapitel-Bezug.
- iOS-Projekt hat Trailing-Dot im Namen → Pfade quoten. Simulator: iPhone 17 Pro Max / iOS 26.2.
