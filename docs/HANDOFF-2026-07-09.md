# HANDOFF — 2026-07-09 (Vollstand, Abend)

> Selbsttragende Übergabe. Zwei Repos: **iOS** (dieses) + **mops-api** (Box/Backend).
> Riesen-Tag. Roter Faden: LV-Doppelzählung lösen → PDF-Herkunft → Summen aufgliedern → geführter Import → **die eine Tür** (alle Unterlagen reinwerfen, Mops sortiert & liest). Alles LIVE + von Andreas gesehen.
> Kundendaten auf USB `INTENSO`/Desktop, NICHT im Repo.

## 1) Repo-Stand

**iOS** — alles Heutige auf **`feature/lv-deckel-typ-b`** (gepusht, KEIN PR), Spitze `24ad37b`:
- `24ad37b` **die eine Tür** — „Alle Unterlagen reinwerfen": Mops sortiert & liest
- `5a43537` Doctype korrigieren (Fehl-Erkennung aufräumen)
- `b900c9c` „Aus Unterlagen übernehmen" — Erschließung → LV (die Brücke)
- `e714012` Löschen Mac-tauglich (sichtbarer Knopf + Alert)
- `f9ad3e1` gespeicherte Auswertung löschen
- `d058da9` PDF-Sprung auch auf Deckel-/Unterpunkt-Zeilen
- `246d6e1` Schritt 4 — Summe automatisch in Unterpunkte aufgliedern
- `aa93673` geführter „Fund-für-Fund"-Import mit PDF-Blick
- `bd68a73` „Was ist abgedeckt?"-Übersicht
- `2eee952` Weg B — Sprung zur gespeicherten Seite · `16a008d` Weg A — Textsuche+Gelb
- `6d91c4e` Deckel-Speichern sichtbar · `b3eaf74` Auflösen per Kontextmenü · `443d3ae` Typ B Deckel · `97512cc` Typ A Export-Fix
- Kette: `feature/lv-bewehrung-dedup` → `fix/lv-export-dedup` → `feature/lv-deckel-typ-b`.

**mops-api** — **`feature/lv-seite-provenance`** (gepusht), Spitze `257a510`, **LIVE auf der Box** (§3). Unverändert seit Mittag.

## 2) Heute gebaut (Kurzfassung)

- **Typ A / Typ B / Weg A / Weg B / Schritt 4** — siehe Vormittags-Historie (Dedup „zählt einmal" überall; Deckel-Modell + manuelles Zusammenführen; PDF-Sprung zur Textstelle/Seite; Summen automatisch aufgliedern). Alles getestet, deployed, bestätigt.
- **Fund-für-Fund-Import** (`aa93673`): Review gruppiert nach Quell-PDF, „im PDF ansehen", „Aufnehmen".
- **„Was ist abgedeckt?"** (`bd68a73`): Positionen nach Herkunft + Prüf-Hinweise; Fußnote „App kennt nur Importiertes".
- **Auswertungen löschen + Doctype korrigieren** (`f9ad3e1`/`e714012`/`5a43537`): falsch geladene löschen; Fehl-Erkennung per Menü korrigieren (nur Etikett, Felder bleiben).
- **Erschließungs-Brücke** (`b900c9c`): `AuswertungLVMapper` — aus gespeicherten `/extract-doc`-Fakten LV-Vorschläge (Erschließung → Hausanschlüsse), Fund-für-Fund bestätigen. **Die wiederverwendbare Brücke** — Bodengutachten/B-Plan/Wohnfläche = später je ein `case`.
- **DIE EINE TÜR** (`24ad37b`): „Alle Unterlagen reinwerfen" (Pläne & Unterlagen). `DateiSortierer` ordnet je Datei über den Namen einem Korb zu (Statik→LV · Unterlagen→Fakten · Rest→ablegen), pro Datei korrigierbar. „Loslegen" fährt automatisch: Statik→`extractPlan`→`mapPositions` (füllt LV mit Deckel/Dedup/Seite), Unterlagen→`extractDoc`→gespeichert; Bilanz am Ende. Reduziert die 3 Einlese-Türen auf eine. Nutzt nur gehärtete Bausteine.

## 3) Deploy-Stand (Box)

- Box `/home/mops/mops-api` (venv, uvicorn:8080) auf **`feature/lv-seite-provenance` = `257a510`** (Weg B seite + Schritt-4 teilgewichte). Live-verifiziert (BoPla untere `teilgewichte=[11.19,27.93,331.28]`).
- **Rückweg:** `git checkout feature/vision-fallback-c1pdf` + `sudo systemctl restart mops-api`.

## 4) Offene Fäden / nächste Schritte

1. **⭐ DWG → Box (bewiesen, nächster Kompass-Punkt):** `dwg2dxf` (LibreDWG) auf die Box + „wenn `.dwg` → erst wandeln"-Schritt vor die Auslese. DWG (binär AC1015) → DXF wurde lokal an echter Setiadji-DWG bewiesen. Dann nimmt „die eine Tür" auch DWG an. Details: [[dwg-dxf-geometrie-direktion]].
2. **DXF-first für Geometrie** (Richtung): Architekten-Grundriss-DXF → Wandlängen → LV-Mengen (der DXF liegt „im Büro", nicht auf der Platte). Größter ungehobener Hebel.
3. **Weitere Doctype→LV-Mappings** (Bodengutachten → Erdbau, B-Plan → Stützwand, Wohnfläche → Mengen): je ein `case` in `AuswertungLVMapper`. ABER: auf Andreas' aktuellen Daten liefern die wenig, weil viele Docs mis-klassifiziert/im falschen Auswerter sind → erst Input sauber (Tür + Typ-korrigieren helfen).
4. **Klassifikations-Qualität:** die `/extract-doc`-Doctype-Erkennung ist schwach (12/15 „auto", 2 falsch). Der `DateiSortierer` (Namens-Heuristik) ist der pragmatische Gegenpol; ggf. Backend-Erkennung schärfen.
5. **Branch-Hygiene:** viele `feature/*` ohne PR; Box auf `feature/lv-seite-provenance` statt kanonischem `vision-fallback` — mergen + zurückstellen. PRs nur mit Andreas' OK.
6. **Landkarte** (Artifact, „Eine Tür, ein Motor") — zeigbar für Raphi/Kunden: nur den oberen Teil zeigen, kein Wort „Doctype".

## 5) Referenzen
- Memos: [[mengenlisten-dedup]] (voller LV-Verlauf), [[dwg-dxf-geometrie-direktion]], [[mops-api-deploy-gotchas]], [[mac-designed-for-ipad-gotchas]], [[prof-zitat-vs-folgerung]], [[stufe-a-zwei-upload-wege]].
- Fachlich: REB-VB 23.003 (Hilfswert = Andreas' Deckel).
- Testdaten: `INTENSO/…/Mustermann-Marktbreit-448-GO/` · DXF: `~/Desktop/mopsss/…/alkisdaten_utm32.dxf` (Gelände), `~/Documents/Bestandspläne DWG_DXF/V4181…Setiadji_Bestand.dxf`.

## 6) Regeln (unverändert)
- Vor jedem Patch `.backup_*`. **Kein Push/PR ohne Andreas' OK** (heute ausdrücklich freigegeben). Conventional Commits + Buch-Kapitelbezug.
- Box-Deploy = `git pull`/`checkout` + `sudo systemctl restart mops-api`, >15s warten. `.env` NICHT anfassen.
- Simulator iPhone 17 Pro Max / iOS 26.2. Keine Kundendaten ins Repo.
- **Wichtig fürs Produkt:** die 3 Einlese-Türen → eine Tür; der ganze Motor bleibt darunter versteckt. „Reinwerfen, fertig" ist der Pitch.
