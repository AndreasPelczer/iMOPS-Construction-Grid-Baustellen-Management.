# HANDOFF — 2026-07-08

> Selbsttragende Übergabe des Tagesstands. Zwei Repos: **iOS** (dieses) + **mops-api** (Box/Backend).
> Alles Wichtige ist auf GitHub gesichert. Kundendaten (Schunder, Setiadji) liegen auf USB/Desktop, NICHT im Repo.

## 1) Repo-Stand

**iOS** (`iMOPS-Construction-Grid-Baustellen-Management.`)
- `main` = `4572e8e` (PR #100). Enthält den kompletten heutigen Tag bis auf die zwei offenen Branches unten.
- **Offen, gepusht, NICHT gemergt (PR auf Andreas' Wort):**
  - `feature/hauslage-platzieren-2b` — Hauslage-Platzieren Schritt 2b (Grundstück anzeigen). Baut, aber **im Test wird das Grundstück noch nicht gezeichnet** — Ursache offen (Box-Restart hatte er evtl. noch nicht, oder Canvas-Mapping). Nächstes Mal: Andreas fragen, ob Fehlermeldung „Konnte nicht laden" kam (→ Box) oder Fläche leer blieb (→ Zeichnen).
  - `feature/lv-bewehrung-dedup` — Bewehrungs-Duplikate im LV gruppieren (Commit `ba94052`). **Fertig & abgenommen**, wartet nur auf PR/Merge.
- Bewusst behaltene Alt-Branches: `claude/clever-clarke-aRgdt`, `codex/mopsiversum-ultradoku` (nicht gemergt, eigene Commits).

**mops-api** (Box, `AndreasPelczer/mops-api`)
- Läuft auf Branch **`feature/vision-fallback-c1pdf`** = `a39f839` (gepusht). **NICHT auf mops-api `main`** — der ganze Vision-Fallback + toleranter Leser + Footprint + Label-Fix lebt seit Wochen live-aber-unmerged auf diesem Branch. **TODO: bei Gelegenheit nach main mergen.**
- Box wurde heute neu gestartet (`systemctl restart mops-api`) → alle neuen Endpoints/Änderungen sind live.

## 2) Heute gebaut (in iOS `main`)
- **Mac-PDF-Crash gefixt** (Metal-API-Validierung im Scheme aus + doppeltes Rendern in CADViewer).
- **Mehrfachauswahl** beim Plan-Import; **Unterlagen sammeln** (PDFs aus mehreren Ordnern vor der Auswertung).
- **Kalkulation:** Positions-Gesamt-Vorschau · Zeilen am Mac löschbar (sichtbarer Papierkorb) · Umschalter „je Einheit / gesamt".
- **Dokument-Auswertung:** speichern & offline wieder aufrufen · Kurz-Zusammenfassung je Dokument · Fakten-Chips in der Ist-Übersicht.
- **B-Plan-Vorgaben-Karte** (Zone/GRZ/GFZ/Höhen/Abgrabung… aus dem ausgewerteten PDF).
- **Geländebrücke:** Max. Aushubtiefe + 🟢/🔴-Ampel gegen die B-Plan-Abgrabung.
- **Alle Inhalts-Sheets als große „Seiten"** (`presentationSizing(.page)`), Mac/iPad.

## 3) Heute gebaut (Backend, `feature/vision-fallback-c1pdf`)
- **Toleranter DXF-Leser** (Höhen übers Höhenband, Layer-Namen egal; kein Absturz bei fehlendem Haus) — endlich versioniert.
- **`/gelaendebruecke/calculate` nimmt optional `footprint`** (JSON [[x,y],…]) → macht haus-lose Bestandspläne rechenbar. Gegen Setiadji getestet (Hand-Footprint → Abtrag 12,9 m³).
- **Neuer `/gelaendebruecke/grundstueck`** — liefert Bbox + Höhenband + Grenzlinien für die App-seitige Hauslage-Platzierung.
- **Label-Fix** (`extract/lv.py`): Bewehrungs-Bezeichnung aus dem **Dateiname** statt der falschen projekt-hartcodierten `STAHL_KG_MAP` → „Bewehrung Ringbalken" statt „Bewehrung ? (Plan None)".

## 4) Offene Fäden / nächste Schritte
1. **Hauslage-MVP Schritt 2b fertig debuggen** — Grundstück zeichnet noch nicht (`feature/hauslage-platzieren-2b`). Danach 2c: Haus-Rechteck platzieren → 4 UTM32-Ecken → `/calculate` mit footprint → Aushub. Details: [[welle-7-gelaendebruecke]] + `docs/Welle7-Hauslage-Platzieren-Spec.md`.
2. **`feature/lv-bewehrung-dedup` mergen** (PR fehlt noch).
3. **mengenlisten-dedup weiterdenken** (optional): gleicher Bauteil mit ABWEICHENDEN kg (z. B. Bodenplatte Plan 933 vs. Liste 879+…) als **Konflikt-Gruppe** darstellen („Werte weichen ab — prüfen"), NICHT auto-summieren. Aktuell gruppiert nur bei **exakt gleicher** Menge (Ringbalken). Referenz + Analyse: [[mengenlisten-dedup]].
4. **mops-api `feature/vision-fallback-c1pdf` → main mergen** (räumt die „live-aber-unmerged"-Lage auf).

## 5) Referenzen
- Memos: [[welle-7-gelaendebruecke]], [[mengenlisten-dedup]], [[mac-designed-for-ipad-gotchas]].
- Specs: `docs/Welle7-Hauslage-Platzieren-Spec.md`, `docs/Unterlagen-Auswerten-Routing-Spec.md`.
- **Präsentation** „Herkunftsnachweis Bewehrungsliste" (Artifact, privat) — erklärt Provenance + Doppelwert + Dedup-Feature.

## 6) Regeln (unverändert)
- Vor jedem Patch `.backup_*`. **Kein Push/PR ohne Andreas' OK.** Conventional Commits, Buch-Kapitelbezug in der Message.
- Box-Deploy = `sudo systemctl restart mops-api` (Andreas' sudo). Trailing-Dot im Projektnamen → Pfade quoten. Simulator iPhone 17 Pro Max / iOS 26.2.
- Keine Kundendaten ins Repo.
